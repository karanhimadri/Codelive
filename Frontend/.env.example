# Frontend Environment Variables
# Copy this file to .env.local for local development
# Configure these in your deployment platform (Vercel, Netlify, etc.)

# Backend API URL (with protocol, no trailing slash)
VITE_API_URL=https://codelive-758504.onrender.com

# Yjs WebSocket URL (domain only, no protocol or /yjs path)
# Protocol (ws:// or wss://) is auto-detected based on page HTTPS status
VITE_YJS_WS_URL=codelive-758504.onrender.com

# For local development, use:
# VITE_API_URL=http://localhost:8080
# VITE_YJS_WS_URL=localhost:8080
